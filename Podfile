# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Google Place Search' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Google Place Search

    pod 'GoogleMaps'
    pod 'GooglePlaces'


  target 'Google Place SearchTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Google Place SearchUITests' do
    # Pods for testing
  end

end
